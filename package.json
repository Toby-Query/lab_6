{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000 & node ./api/index.js"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^0.2.1"
  }
}
